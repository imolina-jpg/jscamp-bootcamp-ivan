<!-- Aquí irá el feedback del ejercicio -->
Excelente trabajo! Agregamos un solo detalle en `models/job.ts` para que haya un filtrado con `limit` y `offset` en la API. Por el resto, todo está bien.

Felicidades!